# Fuzzing

libFuzzer harnesses for the `fx_lib` parsers. Build with `cmake --preset fuzz`;
conventions, smoke runs, and how to add a harness are documented in
[docs/development.md](../docs/development.md#fuzzing).
